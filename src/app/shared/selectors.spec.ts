import * as reducers from './board.selectors';
import { Player } from '../models/player-model';

describe('Board Selectors', () => {
    // it('should reduce state to player has not won', () => {
    //     expect(reducers.hasWonSelector.projector({ game: { hasWon: null } })).toEqual(null);
    // });

    // it('should reduce state to a winning player', () => {
    //     expect(reducers.hasWonSelector.projector(new Player(1, "Winner", "X", "black"))).toEqual(new Player(1, "Winner", "X", "black"));
    // });
});