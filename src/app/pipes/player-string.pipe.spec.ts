import { PlayerStringPipe } from './player-string.pipe';

describe('PlayerStringPipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerStringPipe();
    expect(pipe).toBeTruthy();
  });
});
